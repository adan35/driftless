/**
 * Tokenization lifecycle (Spec 06).
 *
 * <p>Scaffold at Spec 00: declares the module's package root only. The create/activate/suspend/
 * resume/deactivate lifecycle and status propagation are owned by Spec 06.
 */
package io.driftless.tokens;
