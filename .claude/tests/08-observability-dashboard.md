# Spec 08 — Observability & Dashboard — QA Verification

**Role:** Senior QA Engineer (independent verification)
**Build verified:** `JAVA_HOME=java-21` · `./mvnw -B verify` → **BUILD SUCCESS** (full reactor,
05:16 min) and `./mvnw -B -pl app,observability,partner-simulator -am verify` → **BUILD SUCCESS**
(05:00 min, Spotless + JaCoCo gates met). Recon property gate
`io.driftless.recon.LedgerInvariantPropertyTest` **RAN** (2 tests, 39.6 s, Skipped=0) — not skipped.

**Tests added by QA:**
- `app/.../DriftlessApplicationTests#aFailedReconciliationEventTurnsTheZeroDriftGaugeRedEndToEnd`
  — end-to-end honesty proof that the gauge goes RED on drift through the real Spring event wiring.
- `observability/.../MetricsUnitTest#meteredLedgerDelegatesReadMethodsFaithfullyAndDoesNotMeterThem`
  — regression proof that the `@Primary` `MeteredLedger` delegates reads verbatim and meters only `post`.

**Spec-08 suite result:** `MetricsUnitTest` 5/5 · `GrafanaDashboardsTest` 3/3 ·
`DriftlessApplicationTests` 6/6 · `PartnerSimulatorMetricsEndpointTest` 1/1 → **15/15 pass, 0 fail,
0 skipped.** No regression in ledger/idempotency/rules/auth/recon suites (all green, unchanged).

| sr_no | test case (Given/When/Then) | expected | actual | reason_of_failure | db_config |
|-------|-----------------------------|----------|--------|-------------------|-----------|
| 1 | **Given** the booted monolith **When** `/actuator/prometheus` is scraped **Then** it exposes `driftless_recon_drift_amount`, `driftless_ledger_entry_count`, `driftless_ledger_signed_sum_abs_minor`, `driftless_outbox_pending_depth`, `driftless_auth_dangling_partner_reverse`, `driftless_auth_outstanding_partner_obligations`, `driftless_auth_compensating_reversals_total`, `driftless_rules_evaluation_seconds`/`_bucket`, `driftless_rules_decisions_total`, plus `jvm_memory_used_bytes` | all documented `driftless_*` + JVM metrics present, sensible names/labels | PASS (`DriftlessApplicationTests#prometheusScrapeExposesDriftlessMetrics`) | — | Testcontainers postgres:16-alpine, full multi-module Flyway, ddl-auto=validate |
| 2 | **Given** a freshly booted system **When** the drift gauge is read before any recon **Then** it reads `0` and `recon_run_passed=1` (seeded green) | gauge 0, passed 1 | PASS (`MetricsUnitTest#reconGaugesSeedZeroAndReflectRuns`; app IT) | — | SimpleMeterRegistry / Testcontainers |
| 3 | **Given** a correct ledger **When** a real `ReconciliationService.run()` executes **Then** `driftless_recon_drift_amount=0` and `driftless_recon_run_passed=1` in the live exposition | gauge 0, passed 1 | PASS (`DriftlessApplicationTests#reconciliationRunDrivesTheZeroDriftGaugeToZero`) | — | Testcontainers postgres:16-alpine |
| 4 | **HONESTY/GATE.** **Given** the booted app **When** a FAILED `ReconciliationResult` carrying drift=7350 is published as the real Spring event the recon service emits **Then** the live gauge flips to `drift=7350` and `passed=0`, and a subsequent passing run restores `0`/`1` | gauge goes RED then GREEN; proves `ReconMetrics @EventListener` is genuinely wired in the monolith | PASS (`DriftlessApplicationTests#aFailedReconciliationEventTurnsTheZeroDriftGaugeRedEndToEnd` — **QA-added**) | — | Testcontainers postgres:16-alpine |
| 5 | **HONESTY (unit).** **Given** `ReconMetrics` **When** `onReconciliation(failing(4200, 3 stuck))` **Then** drift gauge=4200, passed=0, stuck_outbox=3, fail-run counter +1 | gauge reflects real drift; would show red | PASS (`MetricsUnitTest#reconGaugesSeedZeroAndReflectRuns`) | — | SimpleMeterRegistry |
| 6 | **Given** the full system boots **When** Flyway runs **Then** ledger/idempotency/tokens/auth/recon tables + per-module history tables all exist and Hibernate `ddl-auto=validate` passes | `account, journal_entry, idempotency_record, outbox_event, token, authorization, hold, reconciliation_result` + 5 history tables present | PASS (`DriftlessApplicationTests#everyModuleFlywayTimelineIsApplied` + `contextLoads`) | — | Testcontainers postgres:16-alpine |
| 7 | **Given** the context **When** beans resolve **Then** `Ledger` is `MeteredLedger`, `RuleEngine` is `TimedRuleEngine`, IdempotencyGuard/OutboxWriter/TokenService/ReconciliationService present, `prometheusEndpoint` bean present | `@Primary` decorators are the resolved beans; all modules wired | PASS (`DriftlessApplicationTests#everyModuleBeanAndARealDatasourceAreWired`) | — | Testcontainers postgres:16-alpine |
| 8 | **Given** the rule-engine latency histogram **When** `evaluate` is timed **Then** `driftless_rules_evaluation_seconds_bucket` (with `le`) is emitted enabling `histogram_quantile(0.99,…)` p99 | percentile-histogram buckets present; decisions tagged by `decision`+`reason` | PASS (`MetricsUnitTest#timedRuleEngineRecordsLatencyHistogramAndDecisions`; app IT contains `_bucket`) | — | SimpleMeterRegistry / Testcontainers |
| 9 | **REGRESSION.** **Given** `MeteredLedger` over a delegate **When** `post` accepts vs throws `BalanceInvariantViolation` **Then** postings vs rejected counter increments, exception rethrown unchanged, nothing swallowed | counts correct; balance invariant behavior intact | PASS (`MetricsUnitTest#meteredLedgerCountsAcceptedAndRejectedPosts`) | — | SimpleMeterRegistry |
| 10 | **REGRESSION (QA-added).** **Given** `MeteredLedger` **When** `balanceOf/findTransaction/entriesFor` called **Then** the delegate's exact return is passed through, the same args forwarded, and read paths move no write meters | reads delegate verbatim; no behavioral change | PASS (`MetricsUnitTest#meteredLedgerDelegatesReadMethodsFaithfullyAndDoesNotMeterThem`) | — | SimpleMeterRegistry |
| 11 | **REGRESSION/EXACTLY-ONCE.** **Given** `OutboxEventMetrics` **When** the same `OutboxPublication.id()` is delivered twice (at-least-once redelivery) **Then** the counter increments exactly once; auth/token break-outs + `outbox_published` tagged counters correct | no double-count; relay exactly-once not broken | PASS (`MetricsUnitTest#outboxEventMetricsCountOnceAndBreakOutAuthAndTokens`) | — | SimpleMeterRegistry |
| 12 | **Given** `deploy/grafana/dashboards/zero-drift.json` **When** parsed **Then** valid JSON, title `Driftless — Zero Drift`, uid `driftless-zero-drift`, references `driftless_recon_drift_amount`, `driftless_recon_run_passed`, `driftless_auth_compensating_reversals_total` | flagship references the drift gauge | PASS (`GrafanaDashboardsTest#zeroDriftDashboardIsValidAndReferencesTheDriftGauge`) | — | n/a (file) |
| 13 | **Given** `system-health.json` **When** parsed **Then** valid JSON referencing `driftless_rules_evaluation_seconds_bucket` and `histogram_quantile(0.99,…)` for p99 and `driftless_ledger_postings_total` | p99 panel present | PASS (`GrafanaDashboardsTest#systemHealthDashboardIsValidAndReferencesTheRuleEngineP99`) | — | n/a (file) |
| 14 | **Given** provisioning **When** checked **Then** datasource `prometheus.yml`, `dashboards.yml`, `prometheus/prometheus.yml` present | provisioned-as-code present | PASS (`GrafanaDashboardsTest#provisioningFilesArePresent`) | — | n/a (file) |
| 15 | **Given** `prometheus.yml` **When** inspected **Then** scrapes `app:8080` and `partner-simulator:8081` at `/actuator/prometheus` | both targets, correct path/ports | PASS (manual inspection — confirmed in file) | — | n/a (file) |
| 16 | **Given** the running partner-simulator **When** `/actuator/prometheus` is fetched over real HTTP **Then** 200 OK with Micrometer exposition (`jvm_memory_used_bytes`) | endpoint exposed | PASS (`PartnerSimulatorMetricsEndpointTest#prometheusEndpointIsExposed`) | — | partner-sim WebFlux test |
| 17 | **Given** the relay `OutboxBatchPublisher` **When** it publishes a batch **Then** `publicationListener.onPublished` is fanned out in id order inside the same tx, after `EventPublisher.publish`, before `markPublished` — not altering delivery/ordering | fan-out additive, exactly-once delivery intact | PASS (existing idempotency/outbox suite green; code inspection) | — | Testcontainers postgres |
| 18 | **Given** the recon property gate **When** `./mvnw -B verify` runs **Then** `LedgerInvariantPropertyTest` executes (not skipped) and the global signed sum stays 0 | gate RUNNING, Skipped=0 | PASS (2 tests, 39.6 s) | — | Testcontainers postgres |
