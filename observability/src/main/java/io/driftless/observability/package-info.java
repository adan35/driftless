/**
 * Metrics wiring and dashboard provisioning (Spec 08).
 *
 * <p>Scaffold at Spec 00: declares the module's package root only. Micrometer/Prometheus wiring and
 * the read-only "zero drift" dashboard are owned by Spec 08.
 */
package io.driftless.observability;
