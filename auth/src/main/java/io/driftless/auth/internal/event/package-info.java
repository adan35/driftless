/**
 * Domain events the Spec 03 saga propagates through the Spec 02 transactional outbox, appended in the
 * same transaction as each state change. Payload records only — no logic.
 */
package io.driftless.auth.internal.event;
