/**
 * Auth/capture/reversal saga (Spec 03).
 *
 * <p>Scaffold at Spec 00: declares the module's package root only. The saga orchestration
 * (rule eval, hold placement, bounded partner call, compensating reversal) is owned by Spec 03.
 */
package io.driftless.auth;
