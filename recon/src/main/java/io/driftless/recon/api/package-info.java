/**
 * The published Spec 07 contract: the reconciliation result model the dashboard (Spec 08) reads, the
 * RCA report, and the load-generator profile/result types. These are plain records/enums with no
 * I/O; the implementation lives in {@code io.driftless.recon.internal}.
 */
package io.driftless.recon.api;
