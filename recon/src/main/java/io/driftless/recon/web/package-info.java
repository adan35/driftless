/**
 * Thin REST surface (Spec 07): on-demand reconciliation run, latest/by-id result reads, and the RCA
 * report — the endpoints Spec 08's dashboard consumes. Routing and serialization only.
 */
package io.driftless.recon.web;
