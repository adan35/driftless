/**
 * Rules authored as data (Spec 05): the {@link io.driftless.rules.config.RuleProperties} binding for
 * {@code driftless.rules.*} that the engine compiles into its in-memory form and reloads off the hot
 * path. This is the rule <em>source</em>; it is never consulted during an authorization.
 */
package io.driftless.rules.config;
