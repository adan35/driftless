/**
 * Hot-path limits/velocity/MCC rule engine (Spec 05).
 *
 * <p>Scaffold at Spec 00: declares the module's package root only. The rule evaluation engine is
 * owned by Spec 05.
 */
package io.driftless.rules;
